---
phase: 04-hardening-edge-cases
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/food.ts
autonomous: true
requirements:
  - PARSE-02
must_haves:
  truths:
    - "The LLM system prompt for food extraction includes an expanded SERVING_SIZES reference covering at minimum: 1 cup (liquid vs dry vs leafy), 1 tbsp, 1 tsp, 1 oz, 1 slice bread, 1 large egg, 1 medium apple, 1 shot (spirits), 1 can (soda/beer), 1 banana, 1 medium potato, 1 chicken breast"
    - "The system prompt instructs the LLM to default to cooked weight for grains and pasta when 'raw' is ambiguous"
    - "quantityG values from the LLM for standard references fall within 20% of canonical values (e.g. 1 shot = 30g ±20%, 1 cup cooked rice = 186g ±20%)"
  artifacts:
    - path: "app/lib/food.ts"
      provides: "Expanded SERVING_SIZES constant and updated extractFoodItems system prompt"
      contains: "1 shot"
  key_links:
    - from: "SERVING_SIZES constant"
      to: "extractFoodItems system prompt"
      via: "template literal interpolation"
      pattern: "\\$\\{SERVING_SIZES\\}"
---

<objective>
Expand the SERVING_SIZES reference string in app/lib/food.ts to cover more common serving units that
currently produce imprecise gram estimates, and update the extractFoodItems system prompt to guide
the LLM toward cooked-weight defaults for grains and pasta.

Purpose: Ensure common quantity estimates (1 cup, 1 slice, 1 shot) produce gram values within 20%
of standard references, satisfying Phase 4 success criterion #2.

Output: app/lib/food.ts with an expanded SERVING_SIZES constant and a more instructive system
prompt in extractFoodItems.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand SERVING_SIZES and refine extractFoodItems system prompt</name>
  <files>app/lib/food.ts</files>
  <read_first>
    - app/lib/food.ts — read the full file; understand the SERVING_SIZES constant, extractFoodItems system prompt content, and how SERVING_SIZES is interpolated into the prompt
  </read_first>
  <action>
    After reading app/lib/food.ts in full:

    1. Replace the SERVING_SIZES constant with an expanded version. Keep all existing entries unchanged. Add the following entries:
       - "1 shot (spirits) = 44ml / ~44g"
       - "1 can soda/beer = 355ml / ~355g"
       - "1 medium banana = ~118g"
       - "1 medium potato = ~213g"
       - "1 chicken breast (medium) = ~174g"
       - "1 cup cooked rice = ~186g"
       - "1 cup cooked pasta = ~140g"
       - "1 cup raw oats = ~80g"
       - "1 cup whole milk = ~244g"
       - "1 cup orange juice = ~248g"

    2. Update the system prompt string inside the openai.chat.completions.create call in extractFoodItems. After the existing "Convert all quantities to grams using these references:" line and the ${SERVING_SIZES} interpolation, add the instruction: "For grains and pasta, assume cooked weight unless the user specifies 'dry' or 'uncooked'. When quantity is vague (e.g. 'a bowl', 'some'), estimate conservatively using a single standard serving."

    3. No other changes to extractFoodItems logic, error handling, JSON schema, or model selection.
  </action>
  <verify>
    <automated>cd /home/court/dev/me/diet-app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles with zero new errors
    - `grep "1 shot" app/lib/food.ts` returns a match showing "44ml / ~44g"
    - `grep "chicken breast" app/lib/food.ts` returns a match
    - `grep "cooked weight" app/lib/food.ts` returns a match in the system prompt string
    - The SERVING_SIZES constant contains at least 17 entries (10 new + 7 original)
    - `grep -c "=" app/lib/food.ts | head` is not needed — count entries by scanning the constant block; at minimum "1 shot", "1 can", "1 banana", "1 potato", "chicken breast", "cooked rice", "cooked pasta", "raw oats", "whole milk", "orange juice" are all present
  </acceptance_criteria>
  <done>SERVING_SIZES covers 17+ common serving references; extractFoodItems system prompt instructs cooked-weight default for grains/pasta; TypeScript compiles clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response → quantityG | LLM output is parsed as JSON; quantityG is a number that must not be negative or absurdly large |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | extractFoodItems JSON parse | accept | Existing JSON schema enforces quantityG as number type; LLM cannot inject non-numeric values |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `grep "1 shot" app/lib/food.ts` matches with gram value
- `grep "cooked weight" app/lib/food.ts` matches inside the system prompt string
- Manually submitting "I had a shot of whiskey" to the chat should parse quantityG near 44 (verified after execute)
</verification>

<success_criteria>
Common quantity estimates (1 cup, 1 slice, 1 shot, 1 can) produce gram values within 20% of standard
references because the LLM has explicit canonical weights to reference in its system prompt.
</success_criteria>

<output>
Create `.planning/phases/04-hardening-edge-cases/04-02-SUMMARY.md` when done.
</output>
