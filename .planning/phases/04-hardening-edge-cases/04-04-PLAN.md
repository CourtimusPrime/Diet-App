---
phase: 04-hardening-edge-cases
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/usda.ts
  - .env.example
autonomous: true
requirements:
  - USDA-02
  - USDA-03
must_haves:
  truths:
    - "When USDA returns HTTP 429 (rate limit exceeded), searchUSDA returns null and logs a console.warn with a human-readable message — it does not throw"
    - "When USDA returns HTTP 403 with a body containing 'OVER_RATE_LIMIT' or 'API_KEY', searchUSDA returns null with a logged warning"
    - ".env.example documents USDA_API_KEY with instructions on where to obtain a free registered key and notes the DEMO_KEY 30 req/hr limit"
    - "The USDA_API_KEY env var is used when present; DEMO_KEY is the fallback (existing behavior retained)"
  artifacts:
    - path: "app/lib/usda.ts"
      provides: "HTTP status inspection in httpsGet and rate-limit detection in searchUSDA"
      contains: "RATE_LIMIT"
    - path: ".env.example"
      provides: "USDA_API_KEY documentation"
      contains: "USDA_API_KEY"
  key_links:
    - from: "httpsGet"
      to: "searchUSDA"
      via: "resolved value with statusCode"
      pattern: "statusCode"
---

<objective>
Add HTTP status inspection to the httpsGet helper in usda.ts so that 429 and 403 rate-limit
responses are detected and cause searchUSDA to return null with a warning rather than attempting to
JSON-parse the error body. Also document USDA_API_KEY in .env.example with instructions for
obtaining a free registered key.

Purpose: Satisfy Phase 4 success criterion #3 — USDA DEMO_KEY rate limit (30 req/hr) is handled
gracefully; items beyond the limit are stored with null nutrients rather than erroring.

Output: app/lib/usda.ts with rate-limit detection; .env.example with USDA_API_KEY entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rate-limit detection to httpsGet and searchUSDA</name>
  <files>app/lib/usda.ts</files>
  <read_first>
    - app/lib/usda.ts — read the full file; understand httpsGet's current Promise shape (resolves with parsed JSON, rejects on network error), how searchUSDA calls httpsGet, and how the catch block in searchUSDA currently returns null
  </read_first>
  <action>
    After reading app/lib/usda.ts in full:

    1. Change httpsGet's return type to Promise<{ statusCode: number; body: unknown }>. Update the implementation: in the res.on('end') callback, resolve with { statusCode: res.statusCode ?? 0, body: JSON.parse(data) }. Wrap the JSON.parse in a try/catch; if parsing fails, resolve with { statusCode: res.statusCode ?? 0, body: null }.

    2. Add an exported constant RATE_LIMIT_STATUS_CODES: readonly number[] = [429, 403] immediately after the httpsGet function.

    3. In searchUSDA, after `const data = (await httpsGet(url.toString()))`, change the type of data to the new return type. Add a check: if RATE_LIMIT_STATUS_CODES.includes(data.statusCode), then console.warn('[usda] Rate limit hit (HTTP ' + data.statusCode + ') for query "' + query + '" — storing with null nutrients'), return null (from searchUSDA, not just the loop iteration). This return null exits the entire function early.

    4. Change the existing `data.foods?.length` reference to `(data.body as USDASearchResponse)?.foods?.length` and update the sorted/return logic accordingly to use `(data.body as USDASearchResponse).foods`.

    5. No changes to the catch block in searchUSDA (already returns null on error).
  </action>
  <verify>
    <automated>cd /home/court/dev/me/diet-app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles with zero new errors
    - `grep "statusCode" app/lib/usda.ts` returns at least 3 matches (resolve call, type check, RATE_LIMIT check)
    - `grep "RATE_LIMIT_STATUS_CODES" app/lib/usda.ts` returns at least 2 matches (definition + usage)
    - `grep "429" app/lib/usda.ts` returns a match inside RATE_LIMIT_STATUS_CODES
    - `grep "Rate limit" app/lib/usda.ts` returns a match inside the warning log
    - searchUSDA still returns USDAFood | null (return type signature unchanged)
  </acceptance_criteria>
  <done>httpsGet returns statusCode alongside parsed body; searchUSDA returns null and logs a warning when HTTP 429 or 403 is received from USDA; TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Document USDA_API_KEY in .env.example</name>
  <files>.env.example</files>
  <read_first>
    - .env.example — read the full file to see current documented env vars and formatting conventions before adding the new entry
  </read_first>
  <action>
    After reading .env.example:

    Add a USDA section to .env.example. Place it logically near the API key entries. The section should contain:

    ```
    # USDA FoodData Central API Key
    # Get a free registered key at: https://fdc.nal.usda.gov/api-guide
    # Without a key, DEMO_KEY is used (limit: 30 requests/hr, 3 req/min)
    # A registered key raises the limit to 3600 requests/hr
    USDA_API_KEY=your_usda_key_here
    ```

    Match the comment style (# prefix) and spacing of existing entries in the file.
  </action>
  <verify>
    <automated>grep "USDA_API_KEY" /home/court/dev/me/diet-app/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep "USDA_API_KEY" .env.example` returns a line matching `USDA_API_KEY=`
    - The entry is preceded by at least one comment line explaining DEMO_KEY limits
    - The URL `fdc.nal.usda.gov` appears in a comment above the key
    - No other lines in .env.example were modified (diff shows only addition)
  </acceptance_criteria>
  <done>.env.example documents USDA_API_KEY with the FDC registration URL and DEMO_KEY rate limit warning</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| USDA API HTTP response → httpsGet | statusCode from external API; must be handled without trusting body content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Denial of Service | searchUSDA / httpsGet | mitigate | Rate-limit HTTP status codes (429, 403) return null immediately; meal save proceeds with null nutrients rather than erroring |
| T-04-04b | Information Disclosure | .env.example | accept | .env.example contains placeholder values only; actual keys live in .env (gitignored) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `grep "RATE_LIMIT_STATUS_CODES" app/lib/usda.ts` shows definition and usage
- `grep "USDA_API_KEY" .env.example` shows documented entry with comment
- Simulate a 429 response (mock test or manual curl with exhausted DEMO_KEY) → confirm meal saves with null nutrients rather than erroring
</verification>

<success_criteria>
USDA DEMO_KEY rate limit (HTTP 429) is detected in searchUSDA and returns null gracefully; affected
food items are stored with null nutrients rather than erroring. USDA_API_KEY is documented in
.env.example with instructions to obtain a free registered key.
</success_criteria>

<output>
Create `.planning/phases/04-hardening-edge-cases/04-04-SUMMARY.md` when done.
</output>
