---
phase: "02-mcp-query-layer"
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - ~/.config/Claude/claude_desktop_config.json
autonomous: false
requirements:
  - MCP-09

user_setup:
  - service: claude_desktop
    why: "Claude Desktop must be restarted for new MCP server config to take effect"
    dashboard_config:
      - task: "Quit and relaunch Claude Desktop after config is written"
        location: "System tray / application menu — no dashboard URL"

must_haves:
  truths:
    - "~/.config/Claude/claude_desktop_config.json has an mcpServers.nutrilog entry with command bun pointing at /home/court/dev/me/diet-app/mcp/server.ts"
    - "The config entry includes DATABASE_URL and OPENROUTER_API_KEY env vars (values read from project .env)"
    - "Claude Desktop loads the nutrilog MCP server and lists 9 tools after restart"
    - "get_remaining_targets, get_deficiencies, and get_nutrient_history tools are callable from Claude Desktop"
  artifacts:
    - path: "~/.config/Claude/claude_desktop_config.json"
      provides: "MCP server entry for Claude Desktop"
      contains: "nutrilog"
  key_links:
    - from: "claude_desktop_config.json mcpServers.nutrilog"
      to: "bun /home/court/dev/me/diet-app/mcp/server.ts"
      via: "Claude Desktop subprocess spawn"
      pattern: "\"command\": \"bun\""
---

<objective>
Write the Claude Desktop MCP config entry for nutrilog and verify all 9 tools are accessible from Claude Desktop. This plan wires the completed mcp/server.ts into Claude.ai via the local stdio transport.

Purpose: Without this config, Claude Desktop has no knowledge of the nutrilog MCP server. After this plan, Claude.ai can call all 9 nutrition tools against the Railway database.

Output: Updated ~/.config/Claude/claude_desktop_config.json with mcpServers.nutrilog entry. Verified tool list in Claude Desktop. Phase 2 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mcp-query-layer/02-RESEARCH.md
@.planning/phases/02-mcp-query-layer/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write nutrilog entry to Claude Desktop config (MCP-09)</name>
  <read_first>
    - ~/.config/Claude/claude_desktop_config.json (full file — must preserve existing preferences key exactly)
    - /home/court/dev/me/diet-app/.env (read DATABASE_URL and OPENROUTER_API_KEY values — do NOT hardcode or guess)
  </read_first>
  <action>
Read ~/.config/Claude/claude_desktop_config.json (currently contains only a "preferences" key — no "mcpServers" key). Read .env to extract the literal values of DATABASE_URL and OPENROUTER_API_KEY. Write the updated config file that adds "mcpServers" as a top-level sibling to "preferences".

The mcpServers.nutrilog entry must use:
  - "command": "bun"  — NOT "npx", NOT "tsx", NOT "node"
  - "args": ["/home/court/dev/me/diet-app/mcp/server.ts"]  — absolute path, no --project flag needed
  - "env": { "DATABASE_URL": "<literal value from .env>", "OPENROUTER_API_KEY": "<literal value from .env>" }

The "preferences" object must be preserved byte-for-byte — do not alter any existing preference keys.

Final file structure:
{
  "mcpServers": {
    "nutrilog": {
      "command": "bun",
      "args": ["/home/court/dev/me/diet-app/mcp/server.ts"],
      "env": {
        "DATABASE_URL": "<value>",
        "OPENROUTER_API_KEY": "<value>"
      }
    }
  },
  "preferences": { ...existing content unchanged... }
}

Security: DATABASE_URL and OPENROUTER_API_KEY values are written into a local config file on disk. This is the standard Claude Desktop MCP pattern for local-only integrations. Do not log the values to stdout.
  </action>
  <verify>
    <automated>python3 -c "import json; c=json.load(open('/home/court/.config/Claude/claude_desktop_config.json')); assert 'nutrilog' in c.get('mcpServers',{}); assert c['mcpServers']['nutrilog']['command']=='bun'; assert 'DATABASE_URL' in c['mcpServers']['nutrilog']['env']; print('config valid')"</automated>
  </verify>
  <acceptance_criteria>
    - ~/.config/Claude/claude_desktop_config.json is valid JSON (python3 json.load succeeds)
    - File contains "mcpServers" key at top level
    - mcpServers.nutrilog.command equals "bun" (not "npx" or "tsx")
    - mcpServers.nutrilog.args[0] equals "/home/court/dev/me/diet-app/mcp/server.ts"
    - mcpServers.nutrilog.env.DATABASE_URL is present and non-empty
    - mcpServers.nutrilog.env.OPENROUTER_API_KEY is present and non-empty
    - The existing "preferences" object is still present with all original keys intact
  </acceptance_criteria>
  <done>Claude Desktop config updated with nutrilog MCP server entry. MCP-09 wiring satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Restart Claude Desktop and verify 9 tools load</name>
  <what-built>
    Task 1 wrote the nutrilog MCP server config to ~/.config/Claude/claude_desktop_config.json. Claude Desktop must be restarted to pick up the new server entry. After restart, Claude Desktop spawns `bun /home/court/dev/me/diet-app/mcp/server.ts` as a subprocess and registers its tools.
  </what-built>
  <how-to-verify>
    1. Quit Claude Desktop completely (File > Quit, or kill the process)
    2. Relaunch Claude Desktop
    3. Open a new conversation
    4. Click the tools/integrations icon (hammer icon or MCP indicator in the chat input area)
    5. Confirm "nutrilog" appears in the connected MCP servers list
    6. Confirm these 9 tools are listed:
       - get_daily_nutrition
       - get_goals
       - set_goal
       - list_nutrients
       - log_food
       - get_meals
       - get_remaining_targets
       - get_deficiencies
       - get_nutrient_history
    7. Optional smoke test: ask Claude "call get_daily_nutrition for today" — it should return a JSON object with date and totals keys (all values may be 0 if no food logged today)
    8. Optional smoke test: ask Claude "call get_deficiencies" — should return { date, deficiencies: [], message: 'No goals set...' } if no DailyTarget rows exist yet
  </how-to-verify>
  <resume-signal>Type "verified" if all 9 tools load, or describe the error (e.g. "server fails to connect", "only 6 tools showing", "bun not found")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config file → Claude Desktop process | claude_desktop_config.json is read by Claude Desktop at startup; malformed JSON would prevent server load |
| Claude Desktop → bun subprocess | DATABASE_URL and OPENROUTER_API_KEY passed as env vars to bun process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | DATABASE_URL in claude_desktop_config.json | accept | Config file is local to ~/.config/Claude/ — user-controlled home directory. Standard pattern for local MCP integrations. No remote exposure. |
| T-02-06 | Tampering | claude_desktop_config.json format | mitigate | Validate with python3 json.load after write (Task 1 verify step). Invalid JSON silently prevents server from loading. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
End-to-end phase verification after both plans complete:

1. Tool count in server.ts:
   `grep -c "server.tool(" /home/court/dev/me/diet-app/mcp/server.ts` → 9

2. Config file valid JSON with nutrilog entry:
   `python3 -c "import json; c=json.load(open('/home/court/.config/Claude/claude_desktop_config.json')); print(c['mcpServers']['nutrilog']['command'])"`
   → prints: bun

3. Server starts without errors (quick smoke test):
   `cd /home/court/dev/me/diet-app && timeout 3 bun mcp/server.ts 2>&1 | head -3`
   → should show McpServer startup message or stdin wait (not an import error)

4. Human verification: Claude Desktop lists all 9 nutrilog tools (checkpoint Task 2)

Phase 2 success criteria mapping:
  - SC1 (get_nutrient_totals returns correct sums): get_daily_nutrition tool — verified by calling it against a day with logged meals
  - SC2 (get_deficiencies identifies below-threshold nutrient): get_deficiencies with inline 0.8 threshold
  - SC3 (get_nutrient_history per-day totals): get_nutrient_history with UTC date strings
  - SC4 (set_daily_target → get_remaining_targets reflects immediately): both read DailyTarget table — automatic
  - SC5 (list_nutrient_columns returns 103 columns): list_nutrients tool returns all NUTRIENT_META entries
</verification>

<success_criteria>
- ~/.config/Claude/claude_desktop_config.json contains valid JSON with mcpServers.nutrilog entry
- Entry uses command "bun" with absolute path to mcp/server.ts
- DATABASE_URL and OPENROUTER_API_KEY env vars present in entry
- Existing preferences object unchanged
- Claude Desktop loads nutrilog server after restart
- All 9 tools visible in Claude Desktop tool list
- Phase 2 complete — MCP server running locally, all tools return data from Railway database
</success_criteria>

<output>
Create `.planning/phases/02-mcp-query-layer/02-02-SUMMARY.md` when done.
</output>
