---
phase: "03.2"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/skeleton.tsx
  - components/ui/sonner.tsx
autonomous: true
requirements: [LOG-01]

must_haves:
  truths:
    - "Running npx shadcn add skeleton generates components/ui/skeleton.tsx"
    - "Running npx shadcn add sonner generates components/ui/sonner.tsx with a Toaster export"
  artifacts:
    - path: "components/ui/skeleton.tsx"
      provides: "Skeleton shimmer component"
      contains: "Skeleton"
    - path: "components/ui/sonner.tsx"
      provides: "Toaster wrapper component"
      contains: "Toaster"
  key_links: []
---

<objective>
Install shadcn Skeleton and Sonner components so subsequent plans can import them without a missing-module error.

Purpose: Both components are generated files — they do not exist in the repo until `npx shadcn add` creates them. This plan runs first so Wave 2 tasks can import `@/components/ui/skeleton` and `@/components/ui/sonner` without error.
Output: components/ui/skeleton.tsx, components/ui/sonner.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-log-tab-view-today-s-logged-food-delete-entries-sort-by-chro/03.2-RESEARCH.md
@.planning/phases/03.2-log-tab-view-today-s-logged-food-delete-entries-sort-by-chro/03.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Skeleton and Sonner components</name>
  <files>components/ui/skeleton.tsx, components/ui/sonner.tsx</files>
  <read_first>
    - components/ui/ — scan directory to confirm skeleton.tsx and sonner.tsx are absent before running commands
    - package.json — confirm sonner 2.0.7 is already listed as a dependency (RESEARCH.md verified it is)
  </read_first>
  <action>
    Run these two commands from the project root, in order:

    1. npx shadcn add skeleton
    2. npx shadcn add sonner

    Both commands use the project's existing shadcn config (components.json, new-york preset). Accept all prompts automatically (the commands are non-interactive when components.json is present).

    After both commands complete, verify the generated files exist:
    - components/ui/skeleton.tsx — must contain a function named Skeleton
    - components/ui/sonner.tsx — must contain an export named Toaster

    Do NOT edit the generated files. Do NOT hand-write these components. The shadcn CLI generates the correct implementation; any manual edit risks deviation from the design system.

    Note: sonner npm package is already installed at v2.0.7 per RESEARCH.md. The `npx shadcn add sonner` command adds the wrapper component only — it does not reinstall the npm package.
  </action>
  <verify>
    <automated>grep -l "Skeleton" components/ui/skeleton.tsx && grep -l "Toaster" components/ui/sonner.tsx && echo "PASS: both components generated"</automated>
  </verify>
  <done>components/ui/skeleton.tsx and components/ui/sonner.tsx exist, each containing their named exports (Skeleton, Toaster).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx → file system | shadcn CLI writes generated files into components/ui/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.2-01 | Tampering | npx shadcn add | accept | shadcn is official registry; sonner package verified [OK] by slopcheck in RESEARCH.md; both generate local files only — no network calls at runtime |
| T-03.2-SC | Tampering | npm/pip/cargo installs | accept | sonner@2.0.7 pre-verified [OK] by slopcheck; no new npm packages installed in this plan — shadcn Skeleton is generated code only |
</threat_model>

<verification>
After this plan completes:
- components/ui/skeleton.tsx exists and exports Skeleton
- components/ui/sonner.tsx exists and exports Toaster
- No modifications to existing files
</verification>

<success_criteria>
Both shadcn components are generated and importable. The project still compiles (no TypeScript errors introduced by the generated files).
</success_criteria>

<output>
Create `.planning/phases/03.2-log-tab-view-today-s-logged-food-delete-entries-sort-by-chro/03.2-01-SUMMARY.md` when done.
</output>
