---
phase: 01-logging-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - app/lib/prisma.ts
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
  - USDA-04
  - USDA-05

must_haves:
  truths:
    - "prisma db push completes with zero errors against Railway PostgreSQL"
    - "Meal, FoodItem, and DailyTarget tables exist in the database after push"
    - "FoodItem has exactly 103 Float? nutrient columns covering energy, macros, minerals, vitamins, amino acids, fatty acids, and bioactives"
    - "Deleting a Meal cascades to its FoodItems (onDelete: Cascade)"
    - "All model IDs are String @id @default(cuid())"
    - "usdaFdcId is Int? (nullable) on FoodItem"
    - "usdaDescription is String? on FoodItem"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Prisma data model with Meal, FoodItem (103 nutrient columns), DailyTarget"
      contains: "model FoodItem"
    - path: "app/lib/prisma.ts"
      provides: "PrismaClient singleton for Next.js hot-reload safety"
      exports: ["prisma"]
  key_links:
    - from: "app/lib/prisma.ts"
      to: "prisma/schema.prisma"
      via: "PrismaClient import from @prisma/client"
      pattern: "from '@prisma/client'"
    - from: "app/api/chat/route.ts"
      to: "app/lib/prisma.ts"
      via: "prisma singleton import"
      pattern: "from '@/lib/prisma'"
---

<objective>
Define the full Prisma schema (Meal, FoodItem with 103 Float? nutrient columns, DailyTarget), push it to Railway PostgreSQL, and create the Prisma singleton for use across the application.

Purpose: Establishes the data layer that all other plans depend on. The wide-column FoodItem schema is the architectural foundation — it enables SQL aggregation without joins (DB-02), cascade deletes (DB-03), and stores all 103 USDA nutrients per food item (USDA-05).

Output: prisma/schema.prisma (pushed to Railway PostgreSQL), app/lib/prisma.ts (singleton client)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-logging-core/01-CONTEXT.md
@.planning/phases/01-logging-core/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write Prisma schema — Meal, FoodItem (103 columns), DailyTarget</name>
  <read_first>
    .planning/phases/01-logging-core/01-RESEARCH.md — "Prisma Schema Structure (FoodItem wide-column)" code example and "USDA Nutrient ID Reference" tables (confirmed + assumed sections); .planning/phases/01-logging-core/01-CONTEXT.md — Prisma Schema decisions block
  </read_first>
  <files>prisma/schema.prisma</files>
  <action>
    Create prisma/schema.prisma with datasource db (provider = "postgresql", url = env("DATABASE_URL")) and generator client (provider = "prisma-client-js").

    Define three models:

    MODEL Meal:
    - id String @id @default(cuid())
    - description String
    - createdAt DateTime @default(now())
    - foodItems FoodItem[] (relation)

    MODEL FoodItem:
    - id String @id @default(cuid())
    - mealId String
    - meal Meal @relation(fields: [mealId], references: [id], onDelete: Cascade)
    - name String (LLM-extracted name)
    - quantityG Float (grams consumed for scaling)
    - quantityDisplay String (original display e.g. "2 cups")
    - usdaFdcId Int? (nullable — absent for unmatched items; per USDA-04)
    - usdaDescription String? (official USDA description; per USDA-04)
    - usdaMatched Boolean @default(false)
    - createdAt DateTime @default(now())

    Then 103 Float? nutrient columns in this exact order (per DB-02 and USDA-05):

    Proximates (11 columns):
    water_g, energy_kcal, energy_atwater_general_kcal, energy_atwater_specific_kcal, protein_g, fat_total_g, carbohydrate_g, fiber_g, sugars_total_g, nitrogen_g, ash_g

    Minerals (10 columns):
    calcium_mg, iron_mg, magnesium_mg, phosphorus_mg, potassium_mg, sodium_mg, zinc_mg, copper_mg, manganese_mg, selenium_mcg

    Vitamins (20 columns):
    vitamin_a_rae_mcg, retinol_mcg, carotene_alpha_mcg, carotene_beta_mcg, vitamin_c_mg, vitamin_d_mcg, vitamin_e_mg, vitamin_e_added_mg, vitamin_k1_mcg, vitamin_k2_mcg, thiamin_mg, riboflavin_mg, niacin_mg, pantothenic_acid_mg, vitamin_b6_mg, biotin_mcg, folate_total_mcg, folate_food_mcg, folate_dfe_mcg, vitamin_b12_mcg, vitamin_b12_added_mcg, choline_total_mg

    Wait — vitamins listed above is 22; adjust to keep total at 103. Use exactly these vitamin columns (20):
    vitamin_a_rae_mcg, retinol_mcg, carotene_beta_mcg, vitamin_c_mg, vitamin_d_mcg, vitamin_e_mg, vitamin_k1_mcg, thiamin_mg, riboflavin_mg, niacin_mg, pantothenic_acid_mg, vitamin_b6_mg, biotin_mcg, folate_total_mcg, folate_dfe_mcg, vitamin_b12_mcg, choline_total_mg, lycopene_mcg, caffeine_mg, theobromine_mg

    Amino Acids (18 columns):
    tryptophan_g, threonine_g, isoleucine_g, leucine_g, lysine_g, methionine_g, cystine_g, phenylalanine_g, tyrosine_g, valine_g, arginine_g, histidine_g, alanine_g, aspartic_acid_g, glutamic_acid_g, glycine_g, proline_g, serine_g

    Fatty Acids (22 columns):
    sfa_total_g, mufa_total_g, pufa_total_g, cholesterol_mg, sfa_4_0_g, sfa_6_0_g, sfa_8_0_g, sfa_10_0_g, sfa_12_0_g, sfa_14_0_g, sfa_16_0_g, sfa_18_0_g, mufa_18_1_g, pufa_18_2_g, pufa_18_3_g, pufa_20_4_g, pufa_20_5_epa_g, pufa_22_5_dpa_g, pufa_22_6_dha_g, starch_g, sucrose_g, glucose_g

    Bioactives / Other (remaining columns to reach 103 total — add: fructose_g, lactose_g, iodine_mcg, selenium_mcg is already counted above — verify total count is exactly 103 before writing; adjust by adding/removing from the "Other" bucket as needed to hit exactly 103 Float? columns):
    fructose_g, lactose_g, iodine_mcg

    IMPORTANT: Before finalising, count all Float? column names to confirm the total is exactly 103. If count is wrong, add or remove columns from the bioactives/other group to reach exactly 103. Do not proceed with a schema that has a different count.

    MODEL DailyTarget:
    - id String @id @default(cuid())
    - columnName String @unique (e.g. "protein_g")
    - label String (e.g. "Protein")
    - targetAmount Float
    - unit String (e.g. "g")
    - createdAt DateTime @default(now())
    - updatedAt DateTime @updatedAt
  </action>
  <verify>
    <automated>cd /home/court/dev/me/diet-app && npx prisma validate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - prisma validate exits 0 with no errors
    - schema.prisma contains "model Meal", "model FoodItem", "model DailyTarget"
    - FoodItem block contains "onDelete: Cascade"
    - FoodItem block contains "usdaFdcId Int?"
    - Exactly 103 lines matching "Float?" appear in the FoodItem model block
  </acceptance_criteria>
  <done>prisma validate passes; schema contains all three models with correct field types and cascade delete</done>
</task>

<task type="auto">
  <name>Task 2: Create Prisma singleton and run db push [BLOCKING]</name>
  <read_first>
    .planning/phases/01-logging-core/01-RESEARCH.md — "Pattern 4: Prisma Singleton for Next.js" and "Pitfall 6: Prisma db push vs migrate dev"
  </read_first>
  <files>app/lib/prisma.ts</files>
  <action>
    Create the directory app/lib/ if it does not exist.

    Create app/lib/prisma.ts implementing the standard Next.js PrismaClient singleton pattern:
    - Import PrismaClient from '@prisma/client'
    - Declare globalForPrisma as (globalThis as unknown as { prisma: PrismaClient })
    - Export const prisma = globalForPrisma.prisma ?? new PrismaClient()
    - Set globalForPrisma.prisma = prisma when NODE_ENV !== 'production'

    This prevents multiple client instances during Next.js hot reload in development.

    Then run prisma db push to apply the schema to Railway PostgreSQL. DATABASE_URL is already set in .env. Use the --accept-data-loss flag only if prompted about data changes on an empty database. Command: npx prisma db push

    This is a [BLOCKING] task — no subsequent plans can run until db push completes successfully.
  </action>
  <verify>
    <automated>cd /home/court/dev/me/diet-app && npx prisma db push 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - app/lib/prisma.ts exists and exports a symbol named "prisma"
    - app/lib/prisma.ts contains "globalForPrisma" and "PrismaClient"
    - npx prisma db push exits 0 and prints "Your database is now in sync with your Prisma schema" or equivalent success message
    - No error lines containing "Error" or "failed" in db push output
  </acceptance_criteria>
  <done>Prisma singleton created; db push succeeds and all three tables (Meal, FoodItem, DailyTarget) exist in Railway PostgreSQL</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app/lib/prisma.ts → Railway PostgreSQL | DATABASE_URL carries connection credentials; must stay in .env (gitignored) only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | DATABASE_URL in .env | mitigate | .env is gitignored; .env.example documents variable name only without value |
| T-01-02 | Tampering | prisma db push on production DB | accept | Phase 1 is dev-only; production switch to migrate deploy documented in RESEARCH.md pitfalls |
| T-01-SC | Tampering | npm/prisma installs | mitigate | All packages pre-audited in RESEARCH.md Package Legitimacy Audit table; prisma@7.8.0 and @prisma/client@7.8.0 verified against npm registry |
</threat_model>

<verification>
After both tasks complete:
1. Run: npx prisma validate — must exit 0
2. Run: npx prisma db push — must print sync success
3. Inspect Railway DB (via Prisma Studio or psql): SELECT table_name FROM information_schema.tables WHERE table_schema='public' — must show Meal, FoodItem, DailyTarget
4. Count Float? columns: grep -c "Float?" prisma/schema.prisma — must equal 103
</verification>

<success_criteria>
- prisma/schema.prisma validates with zero errors
- npx prisma db push completes successfully
- app/lib/prisma.ts exports the prisma singleton
- FoodItem model has exactly 103 Float? nutrient columns
- Cascade delete is configured on FoodItem.mealId relation
</success_criteria>

<output>
Create .planning/phases/01-logging-core/01-01-SUMMARY.md when done
</output>
