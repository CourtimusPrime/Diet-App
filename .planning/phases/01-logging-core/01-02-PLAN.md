---
phase: 01-logging-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/usda.ts
autonomous: true
requirements:
  - USDA-01
  - USDA-02
  - USDA-03
  - USDA-05

must_haves:
  truths:
    - "NUTRIENT_ID_TO_COLUMN maps all 103 nutrient IDs to their Float? column names"
    - "searchUSDA() fetches top 5 results with Foundation and SR Legacy data types and returns the highest-priority match"
    - "searchUSDA() retries without the raw suffix when the first query returns zero results"
    - "nutrientsToColumns() scales USDA per-100g values by (quantityG / 100) before returning"
    - "Zero USDA results returns null without throwing"
    - "Unknown nutrient IDs from USDA response are silently ignored"
  artifacts:
    - path: "app/lib/usda.ts"
      provides: "NUTRIENT_ID_TO_COLUMN map, searchUSDA(), nutrientsToColumns()"
      exports: ["NUTRIENT_ID_TO_COLUMN", "searchUSDA", "nutrientsToColumns"]
  key_links:
    - from: "app/api/chat/route.ts"
      to: "app/lib/usda.ts"
      via: "named imports"
      pattern: "from '@/lib/usda'"
---

<objective>
Implement app/lib/usda.ts — the complete USDA FoodData Central integration module containing the NUTRIENT_ID_TO_COLUMN lookup map, searchUSDA() fetch helper, and nutrientsToColumns() scaling function.

Purpose: This is the highest-complexity module in Phase 1. The NUTRIENT_ID_TO_COLUMN map must exactly mirror the Float? columns in prisma/schema.prisma (established in Plan 01-01). The search and scaling logic is the bridge between raw USDA API data and the typed Prisma writes in the chat route.

Output: app/lib/usda.ts with all exports consumed by app/api/chat/route.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-logging-core/01-CONTEXT.md
@.planning/phases/01-logging-core/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build NUTRIENT_ID_TO_COLUMN map and TypeScript types</name>
  <read_first>
    .planning/phases/01-logging-core/01-RESEARCH.md — "USDA Nutrient ID Reference" section (both "Confirmed via Live API" and "Assumed Additional Nutrients" tables); .planning/phases/01-logging-core/01-RESEARCH.md — "NUTRIENT_ID_TO_COLUMN Map Structure" code example; prisma/schema.prisma — read the FoodItem model to get the exact Float? column names (the map must be a 1:1 match)
  </read_first>
  <files>app/lib/usda.ts</files>
  <behavior>
    - NUTRIENT_ID_TO_COLUMN[1003] === 'protein_g'
    - NUTRIENT_ID_TO_COLUMN[1008] === 'energy_kcal'
    - NUTRIENT_ID_TO_COLUMN[2047] === 'energy_atwater_general_kcal'
    - NUTRIENT_ID_TO_COLUMN[1210] === 'tryptophan_g'
    - NUTRIENT_ID_TO_COLUMN[1258] === 'sfa_total_g'
    - Object.keys(NUTRIENT_ID_TO_COLUMN).length === 103 (one entry per schema column)
    - Every value in NUTRIENT_ID_TO_COLUMN is a key that exists as a Float? column in schema.prisma
    - No duplicate values in the map (each column name appears at most once)
  </behavior>
  <action>
    Create app/lib/usda.ts.

    Define the USDAFoodNutrient interface: { nutrientId: number; value: number | null }.
    Define the USDAFood interface: { fdcId: number; description: string; dataType: string; foodNutrients: USDAFoodNutrient[] }.
    Define the USDASearchResponse interface: { foods: USDAFood[] }.

    Export NUTRIENT_ID_TO_COLUMN as Record&lt;number, string&gt;. Build the map by reading prisma/schema.prisma to get the exact list of Float? column names, then assigning the correct USDA 4-digit nutrient ID to each column name. Use the ID tables from RESEARCH.md as the authority.

    For the two energy IDs that both map to energy_kcal (1008 and 2047), map:
    - 1008 → 'energy_kcal'
    - 2047 → 'energy_atwater_general_kcal'
    (Both are separate columns in the schema — do not alias them to the same column name.)

    For any schema column where the exact USDA nutrient ID is [ASSUMED] in RESEARCH.md, include the ID as documented — the implementation task note in RESEARCH.md instructs that gaps will be verified with a registered key during dev. Do not omit columns for lack of ID confidence.

    After writing the map, count: Object.keys(NUTRIENT_ID_TO_COLUMN).length must equal 103.

    Export CORE_NUTRIENT_COLUMNS as string[] = ['energy_kcal', 'protein_g', 'fat_total_g', 'carbohydrate_g', 'fiber_g'].
    Export DATA_TYPE_PRIORITY as string[] = ['Foundation', 'SR Legacy', 'Survey (FNDDS)', 'Branded'].

    Do not implement searchUSDA or nutrientsToColumns in this task — those are Task 2.
  </action>
  <verify>
    <automated>cd /home/court/dev/me/diet-app && npx tsx -e "import { NUTRIENT_ID_TO_COLUMN } from './app/lib/usda.ts'; console.log(Object.keys(NUTRIENT_ID_TO_COLUMN).length)"</automated>
  </verify>
  <acceptance_criteria>
    - app/lib/usda.ts exports NUTRIENT_ID_TO_COLUMN, CORE_NUTRIENT_COLUMNS, DATA_TYPE_PRIORITY
    - NUTRIENT_ID_TO_COLUMN has exactly 103 entries
    - No duplicate values in NUTRIENT_ID_TO_COLUMN (each column name is unique)
    - Every value in NUTRIENT_ID_TO_COLUMN matches a Float? column name in prisma/schema.prisma
    - USDAFoodNutrient, USDAFood, USDASearchResponse interfaces are exported
  </acceptance_criteria>
  <done>NUTRIENT_ID_TO_COLUMN map is complete with 103 entries mirroring the Prisma schema exactly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement searchUSDA() and nutrientsToColumns()</name>
  <read_first>
    .planning/phases/01-logging-core/01-RESEARCH.md — "Pattern 2: USDA FoodData Central Search + Nutrient Extraction" code example; .planning/phases/01-logging-core/01-CONTEXT.md — "USDA Integration & Error Handling" decisions block; app/lib/usda.ts (Task 1 output — read for existing types and exports)
  </read_first>
  <files>app/lib/usda.ts</files>
  <behavior>
    - searchUSDA('chicken breast') appends ' raw' first: fetches /fdc/v1/foods/search?query=chicken+breast+raw
    - searchUSDA returns the Foundation-dataType result when both Foundation and SR Legacy are in results
    - searchUSDA retries without ' raw' when first query returns empty foods array
    - searchUSDA returns null (not throw) when both queries return empty results
    - nutrientsToColumns scales by (quantityG / 100): protein 20g at 50g quantity → 10g stored
    - nutrientsToColumns returns empty object {} when food has no matching nutrient IDs
    - nutrientsToColumns ignores nutrient IDs not present in NUTRIENT_ID_TO_COLUMN (no crash, no key added)
    - nutrientsToColumns skips entries where nutrient.value is null
  </behavior>
  <action>
    Add to app/lib/usda.ts (do not replace existing content from Task 1):

    Export async function searchUSDA(foodName: string): Promise&lt;USDAFood | null&gt;.
    - USDA_BASE = 'https://api.nal.usda.gov/fdc/v1'
    - apiKey = process.env.USDA_API_KEY ?? 'DEMO_KEY'
    - Queries array: [`${foodName} raw`, foodName] — try in order, return first non-empty result
    - For each query, build URL with URLSearchParams: query, dataType ('Foundation,SR Legacy,Survey (FNDDS),Branded'), pageSize ('5'), api_key
    - On response: parse JSON as USDASearchResponse
    - If data.foods?.length > 0: sort by DATA_TYPE_PRIORITY index (Foundation index 0 beats SR Legacy index 1), return sorted[0]
    - If empty: continue to next query. After both queries empty: return null
    - On fetch error or non-ok response: log to console.error and return null (never throw — zero-results must not block meal save per USDA-01)

    Export function nutrientsToColumns(food: USDAFood, quantityG: number): Record&lt;string, number&gt;.
    - Initialize result as empty Record&lt;string, number&gt;
    - Iterate food.foodNutrients
    - For each nutrient: look up NUTRIENT_ID_TO_COLUMN[nutrient.nutrientId]
    - If column name found and nutrient.value != null: result[columnName] = (nutrient.value * quantityG) / 100
    - Return result
    - This function is pure (no network calls) and synchronous
  </action>
  <verify>
    <automated>cd /home/court/dev/me/diet-app && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit --skipLibCheck exits 0 for app/lib/usda.ts
    - searchUSDA is exported as an async function
    - nutrientsToColumns is exported as a synchronous function
    - searchUSDA body contains the string 'raw' (suffix strategy present)
    - searchUSDA body contains 'return null' (graceful failure path present)
    - nutrientsToColumns body contains '/ 100' (scaling present)
    - nutrientsToColumns body contains 'NUTRIENT_ID_TO_COLUMN' (map lookup present)
  </acceptance_criteria>
  <done>searchUSDA and nutrientsToColumns are implemented, type-safe, and handle all error paths gracefully</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| USDA API response → nutrientsToColumns | External API response is parsed; only known nutrient IDs are mapped |
| USDA_API_KEY in .env | API key must not be passed to client; usda.ts is server-only module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | USDA_API_KEY | mitigate | Accessed only via process.env in server-side module; Next.js route handler keeps it server-side; DEMO_KEY is safe to expose but USDA_API_KEY must not appear in client bundles |
| T-01-04 | Tampering | USDA API response injection | mitigate | nutrientsToColumns only reads known nutrientId keys; unknown IDs are silently skipped; no eval or dynamic SQL construction |
| T-01-05 | Denial of Service | USDA DEMO_KEY rate limit | accept | Phase 1 dev-only; 30 req/hr sufficient for testing; RESEARCH.md notes registered key upgrade path; graceful null return prevents crash |
| T-01-SC | Tampering | npm installs | mitigate | No new packages in this plan; usda.ts uses built-in fetch only |
</threat_model>

<verification>
After both tasks complete:
1. TypeScript check: npx tsc --noEmit --skipLibCheck — zero errors in app/lib/usda.ts
2. Map count: grep -c "': '" app/lib/usda.ts — should reflect 103 entries in NUTRIENT_ID_TO_COLUMN
3. Manual spot-check: confirm NUTRIENT_ID_TO_COLUMN[1003] === 'protein_g', [1258] === 'sfa_total_g'
4. Grep for error handling: grep -c "return null" app/lib/usda.ts — should be at least 2 (one per error path)
</verification>

<success_criteria>
- app/lib/usda.ts compiles with zero TypeScript errors
- NUTRIENT_ID_TO_COLUMN has exactly 103 entries with no duplicate values
- searchUSDA handles empty results and fetch errors without throwing
- nutrientsToColumns scales nutrients from per-100g to consumed quantity correctly
</success_criteria>

<output>
Create .planning/phases/01-logging-core/01-02-SUMMARY.md when done
</output>
