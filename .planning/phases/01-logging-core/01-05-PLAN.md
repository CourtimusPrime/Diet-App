---
phase: 01-logging-core
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app/globals.css
  - .env.example
  - next.config.ts
  - tsconfig.json
  - postcss.config.mjs
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Next.js 14 App Router project is initialised and npm run dev starts without errors"
    - "Shadcn/ui New York preset is initialised and components/ui/ directory exists"
    - "Required Shadcn components are installed: button, card, badge, collapsible, textarea, separator"
    - "next-themes is installed (next-themes@0.4.6)"
    - "lucide-react is installed (lucide-react@1.17.0)"
    - "openai SDK is installed (openai@6.41.0)"
    - "prisma and @prisma/client are installed (7.8.0)"
    - ".env.example documents DATABASE_URL, OPENROUTER_API_KEY, USDA_API_KEY with descriptions"
    - "globals.css contains Shadcn CSS variables for both light and dark mode"
    - "OPENROUTER_API_KEY is NOT in .env — it is documented in .env.example only (gitignored .env)"
  artifacts:
    - path: "package.json"
      provides: "All production dependencies installed"
      contains: "next-themes"
    - path: "app/globals.css"
      provides: "Shadcn CSS variables, Tailwind base, mobile-first overrides"
    - path: ".env.example"
      provides: "Documentation of all required environment variables"
      contains: "OPENROUTER_API_KEY"
    - path: "components/ui/"
      provides: "Shadcn component files (button, card, badge, collapsible, textarea, separator)"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import '@/app/globals.css'"
      pattern: "globals.css"
    - from: "app/components/ChatInterface.tsx"
      to: "components/ui/button"
      via: "import { Button } from '@/components/ui/button'"
      pattern: "components/ui/button"
---

<objective>
Bootstrap the Next.js 14 project: install all dependencies, initialise Shadcn/ui with New York preset, add required Shadcn component files, write globals.css with Shadcn variables, and document all environment variables in .env.example.

Purpose: This is the foundation that all other Wave 1 plans depend on. Without installed packages (openai, prisma, next-themes, lucide-react) and the Shadcn component files, no other plan can compile. Plans 01-01, 01-02, and 01-03 can execute in parallel with this plan because they only create source files — the compile step comes in Wave 2+.

Output: Bootstrapped project with all dependencies, Shadcn components, globals.css, and .env.example
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-logging-core/01-CONTEXT.md
@.planning/phases/01-logging-core/01-RESEARCH.md
@.planning/phases/01-logging-core/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialise Next.js project and install all dependencies</name>
  <read_first>
    .planning/phases/01-logging-core/01-RESEARCH.md — "Standard Stack" tables (exact package versions); .planning/phases/01-logging-core/01-RESEARCH.md — "Package Legitimacy Audit" table (all packages pre-approved); .planning/phases/01-logging-core/01-RESEARCH.md — Installation block; .planning/phases/01-logging-core/01-CONTEXT.md — "Prisma Schema & DB Setup" (prisma@7.8.0 confirmed)
  </read_first>
  <files>package.json, next.config.ts, tsconfig.json, postcss.config.mjs</files>
  <action>
    Check whether a Next.js project already exists in the working directory by reading package.json. If package.json exists with a "next" dependency, skip project creation and go directly to dependency installation.

    If no Next.js project exists: run the Next.js 14 project scaffolding. Because this is a greenfield project, use:
    npx create-next-app@14 . --typescript --tailwind --eslint --app --src-dir=no --import-alias="@/*"
    Accept all prompts with defaults.

    After scaffolding (or if project already exists), install the exact versions from RESEARCH.md Package Legitimacy Audit:
    npm install next-themes@0.4.6
    npm install openai@6.41.0
    npm install lucide-react@1.17.0
    npm install prisma@7.8.0 @prisma/client@7.8.0

    These packages are all pre-approved in RESEARCH.md — no checkpoint needed.

    After installation, verify package.json contains all five: next-themes, openai, lucide-react, prisma, @prisma/client.

    Initialise Prisma (creates prisma/schema.prisma placeholder and sets up .env with DATABASE_URL):
    npx prisma init --datasource-provider postgresql

    Note: If .env already contains DATABASE_URL (Railway is already configured per STATE.md), do NOT overwrite it. Check first with: grep DATABASE_URL .env 2>/dev/null.
  </action>
  <verify>
    <automated>cd /home/court/dev/me/diet-app && node -e "const p = require('./package.json'); ['next-themes','openai','lucide-react','prisma','@prisma/client'].forEach(d => { if (!p.dependencies[d]) throw new Error('Missing: '+d); }); console.log('All dependencies present')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json exists with dependencies section containing: next (14.x), next-themes (0.4.6), openai (6.41.0), lucide-react (1.17.0), prisma (7.8.0), @prisma/client (7.8.0)
    - node_modules/ directory exists
    - tsconfig.json exists with paths alias "@/*"
    - prisma/schema.prisma exists (may be placeholder — Plan 01-01 writes the real content)
    - .env contains DATABASE_URL (not overwritten from existing Railway connection)
  </acceptance_criteria>
  <done>All dependencies installed; Next.js project structure exists; Prisma initialised</done>
</task>

<task type="auto">
  <name>Task 2: Initialise Shadcn/ui and add globals.css + .env.example</name>
  <read_first>
    .planning/phases/01-logging-core/01-UI-SPEC.md — Design System table (New York preset), Color section (CSS variables approach), Component Inventory table (all six components to add); .planning/phases/01-logging-core/01-RESEARCH.md — "Note: Shadcn/ui itself is not an npm package" (installed via CLI); .planning/phases/01-logging-core/01-CONTEXT.md — "Chat UI Layout & Theming" (shadcn init command)
  </read_first>
  <files>app/globals.css, .env.example, components/ui/ (directory created by shadcn init)</files>
  <action>
    Run Shadcn/ui initialisation with New York style preset (per CONTEXT.md decision):
    npx shadcn@latest init --defaults

    When prompted (or via defaults flag): select New York style, zinc base color, CSS variables (not utility classes). If the CLI is interactive and --defaults doesn't fully suppress prompts, respond: style=New York, base color=Zinc, CSS variables=yes.

    After init completes, add all six required Shadcn components from the UI-SPEC Component Inventory table in a single command:
    npx shadcn@latest add button card badge collapsible textarea separator

    Verify the components/ui/ directory now contains: button.tsx, card.tsx, badge.tsx, collapsible.tsx, textarea.tsx, separator.tsx.

    Inspect the generated app/globals.css (or globals.css at root). It should already contain Shadcn CSS variable declarations for :root (light) and .dark (dark mode). If globals.css is missing Shadcn variables, the shadcn init step failed — re-run it.

    Append to app/globals.css (after the Shadcn-generated content — do not overwrite):
    Add a comment block "/* NutriLog custom */" followed by:
    - html, body { height: 100%; } (ensures full-screen layout works)
    - No additional custom styles needed in Phase 1 — Tailwind utilities handle the rest

    Create .env.example in the project root with the following content documenting all required environment variables (values are placeholders, not real keys — this file IS committed to git):

    DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"
    # Railway PostgreSQL connection string — find in Railway dashboard > project > variables

    OPENROUTER_API_KEY="sk-or-..."
    # OpenRouter API key from openrouter.ai/keys
    # Required for LLM food parsing (google/gemini-2.0-flash-001) and confirmation messages
    # Add to .env (gitignored) — never commit this value

    USDA_API_KEY="DEMO_KEY"
    # USDA FoodData Central API key from https://api.data.gov/signup/
    # DEMO_KEY works for dev (30 req/hr); register for 1,000 req/hr
    # Optional — DEMO_KEY is used as fallback in app/lib/usda.ts

    IMPORTANT: Verify that .env is listed in .gitignore. If not present, add .env to .gitignore. .env.example is intentionally committed.
  </action>
  <verify>
    <automated>cd /home/court/dev/me/diet-app && ls components/ui/button.tsx components/ui/card.tsx components/ui/badge.tsx components/ui/collapsible.tsx components/ui/textarea.tsx components/ui/separator.tsx && echo "All components present"</automated>
  </verify>
  <acceptance_criteria>
    - components/ui/button.tsx exists
    - components/ui/card.tsx exists
    - components/ui/badge.tsx exists
    - components/ui/collapsible.tsx exists
    - components/ui/textarea.tsx exists
    - components/ui/separator.tsx exists
    - app/globals.css contains '--background' (Shadcn CSS variable present)
    - app/globals.css contains '.dark' block (dark mode variables present)
    - .env.example exists and contains 'OPENROUTER_API_KEY' and 'DATABASE_URL' and 'USDA_API_KEY'
    - .env is listed in .gitignore: grep -c "^\.env$" .gitignore >= 1
    - .env.example does NOT contain a real API key (value is placeholder "sk-or-..." or similar)
  </acceptance_criteria>
  <done>Shadcn/ui initialised with New York preset; all 6 component files installed; globals.css has Shadcn CSS variables; .env.example documents all three environment variables</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → process.env | Real credentials live in .env (gitignored); .env.example is safe for git |
| shadcn CLI output | Components copied from official Shadcn registry; no third-party registries per UI-SPEC Registry Safety section |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Information Disclosure | .env committed to git | mitigate | .env in .gitignore verified as acceptance criterion; .env.example contains only placeholder values |
| T-01-15 | Tampering | shadcn components from third-party registry | mitigate | UI-SPEC Registry Safety confirms only official shadcn registry used; npx shadcn@latest pulls from registry.shadcn.ui only |
| T-01-SC | Tampering | npm/shadcn installs | mitigate | All npm packages pre-audited in RESEARCH.md; shadcn uses official registry (not third-party); create-next-app@14 is Vercel official |
</threat_model>

<verification>
After both tasks complete:
1. Dependency check: node -e "require('./package.json').dependencies" — all five packages must appear
2. Component check: ls components/ui/ — must list button.tsx, card.tsx, badge.tsx, collapsible.tsx, textarea.tsx, separator.tsx
3. CSS check: grep -c "background" app/globals.css — must be > 0 (Shadcn variables present)
4. .env safety: grep "^\.env$" .gitignore — must match (gitignored)
5. .env.example safety: grep "OPENROUTER_API_KEY" .env.example — must be present; grep "sk-or-v1-[a-z0-9]" .env.example must NOT match (no real key)
6. Dev server: npm run dev — must start without errors and serve on localhost:3000
</verification>

<success_criteria>
- npm run dev starts without errors
- All 6 Shadcn components installed in components/ui/
- All required npm dependencies at correct versions
- .env.example committed with all three variables documented
- .env gitignored and DATABASE_URL preserved from Railway setup
</success_criteria>

<output>
Create .planning/phases/01-logging-core/01-05-SUMMARY.md when done
</output>
